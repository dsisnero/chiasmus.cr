{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "chiasmus": {
      "type": "local",
      "command": ["/bin/sh", "-c", "cd \"$(git rev-parse --show-toplevel 2>/dev/null || echo /tmp)\" && exec ./bin/chiasmus"],
      "enabled": true,
      "timeout": 120000
    }
  }
}
