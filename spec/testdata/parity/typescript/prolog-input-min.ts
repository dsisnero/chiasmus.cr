// Derived from vendor/chiasmus/src/formalize/prolog-input.ts to isolate
// top-level function parity without incidental helper-call drift.
function extractPrologQuery(_spec: string): string {
  return "";
}
